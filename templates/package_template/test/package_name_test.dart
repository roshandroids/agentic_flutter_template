import 'package:package_name/package_name.dart';
import 'package:test/test.dart';

void main() {
  test('package_name exports its public surface', () {
    expect(const PackageNamePlaceholder(), isNotNull);
  });
}
