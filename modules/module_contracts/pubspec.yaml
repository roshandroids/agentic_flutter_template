name: module_contracts
description: >
  Pure-Dart contracts every pluggable modules/* integration implements
  (auth, analytics, notifications, payments). No provider SDK dependency -
  see modules/README.md for the contract pattern.
publish_to: "none"
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  core:
    path: ../../packages/core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
