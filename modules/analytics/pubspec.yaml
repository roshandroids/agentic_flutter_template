name: analytics
description: >
  Reference implementation of module_contracts' AnalyticsModule - logs to
  the console. Real provider modules (Firebase/Segment/PostHog/...) follow
  this exact shape; see modules/README.md.
publish_to: "none"
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  module_contracts:
    path: ../module_contracts

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
