# Changelog (workspace)

This top-level changelog tracks changes to the template/workspace itself
(new packages, new CI workflows, scaffold changes) - each `packages/*`
package maintains its own `CHANGELOG.md`, generated by `melos version`, for
its own release history.

## Unreleased

- Initial scaffold: workspace, `.ai/` AI architecture, modular CI/CD,
  `packages/core|network|local_storage|design_system`, `apps/app` with one
  demonstration feature (`dashboard`).
