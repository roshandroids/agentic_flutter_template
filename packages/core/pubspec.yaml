name: core
description: >
  Domain-layer foundation shared by every package and app in this workspace:
  Result/Failure error handling, and the storage/network abstractions
  concrete implementations bind to. Zero Flutter dependency by design - see
  docs/adr/ADR-0004-result-failure-error-handling.md and
  docs/architecture/DEPENDENCY_RULES.md.
publish_to: "none"
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
